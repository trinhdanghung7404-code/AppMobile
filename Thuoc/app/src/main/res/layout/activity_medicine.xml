<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- Tiêu đề -->
    <TextView
        android:id="@+id/tvTitleMedicine"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Danh sách thuốc"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:gravity="center"
        android:padding="16dp"
        android:background="#F3F3F3" />

    <!-- Danh sách thuốc -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewMedicine"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/tvTitleMedicine"
        android:layout_above="@id/bottomNavigation"
        android:padding="16dp" />

    <!-- 🔴 Nút XÓA thuốc (màu đỏ), nằm trên nút Thêm -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabDeleteMedicine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_above="@id/fabAddMedicine"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        app:srcCompat="@android:drawable/ic_menu_delete"
        app:tint="@android:color/white"
        android:backgroundTint="@android:color/holo_red_dark"
        android:contentDescription="Xóa thuốc đã chọn" />

    <!-- 🟢 Nút THÊM thuốc -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddMedicine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_above="@id/bottomNavigation"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        app:srcCompat="@android:drawable/ic_input_add"
        app:tint="@android:color/white"
        android:backgroundTint="@android:color/holo_green_dark"
        android:contentDescription="Thêm thuốc mới" />

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_alignParentBottom="true"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconSize="40dp"
        app:labelVisibilityMode="unlabeled"
        android:background="#EDE1F6"
        android:elevation="8dp" />

    <!-- Nút Tài khoản tròn nổi giữa -->
    <ImageButton
        android:id="@+id/btnAccount"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="28dp"
        android:background="@drawable/bg_circle_white"
        android:src="@drawable/ic_account"
        android:scaleType="centerInside"
        android:elevation="16dp" />

</RelativeLayout>
